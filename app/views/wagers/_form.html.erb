<%= form_with(model: wager, local: true) do |form| %>
  <% if wager.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(wager.errors.count, "error") %> prohibited this wager from being saved:</h2>

      <ul>
      <% wager.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :challenge_id %>
    <%= form.text_field :challenge_id %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
